# Chultracrap

Chultracrap is an exploit for ChromeOS devices that exploits a technical vulnerability in RMA shims to unenroll and be able to do whatever you want with it, (The Chromebook)

# Chultracrap is an exploit for ChromeOS kernel (or kernver version 3 or lower.)

## What Kernver and ChromeOS version do I have?
To verify your kernver and your ChromeOS version, you need to first boot ChromeOS and then press `alt+v`, you will find a number followed by other numbers, only the first 2 or 3 numbers matter, such as `94`, `110` or `127`. Take note of this number and find the method related. If it is `120` or higher, you should check your kernver. Press `esc+⟳+⏻ ` (`esc+refresh+power`), and then `tab`.

# Your screen should look like This:

<img src="https://encrypted-tbn0.gstatic.com/images?q=tbn:ANd9GcSvmDsUJCwFwM07_716NVQGZK9LW66zvlLpKdIopqQNzA&amp;s=10" alt="Chromebook Recovery black screen after USB recovery is inserted - Chromebook Community"/>
